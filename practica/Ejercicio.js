function Par(numero) {
    return numero % 2 === 0;
}

console.log(Par(4)); // devuelve verdadero por que el digito es un numero par

console.log(Par(9));  //devuelve falso por que el numero es impar si este fuese un numero par daria verdadero 
