//esta funcion formaremos como parametros de numeros: a y b, que nos devolvera la suma de los dos numeros

//declaracion
function sumar(a,b) {
    return a + b;
}

// ahora la sustitucion con numeros
let resultadosuma = sumar (5,3);
console.log("la suma de 5 y 3 es: ", resultadosuma)
