// para esta funcion tomaremos dos parametros que serian el ancho y el alto donde nos van a devolver el area del retangulo

function calcularArea(ancho, alto) {
    return ancho*alto;
}

//aplicamos las medidas 
let area = calcularArea(10,20);
console.log(" El area del retangulo es:", area);